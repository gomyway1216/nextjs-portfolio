# The next shogi-strength experiment: discard one-sided self-play and relearn hard high-rated positions at depth 18

> HalfKP81 v4 finished its fresh 56-game screen at 29 wins, 24 losses, and three draws: 61/112, one half-point below the preregistered 62/112 line. The point estimate moved upward, but the candidate was rejected. The successor is not a rescue of that model. It preserves the broad direct-teacher data, selects 8,192 hard parents from 800,000 high-rated game positions, and labels their sibling moves afresh with YaneuraOu at depth 18. [日本語](./blog-shogi-halfkp81-hard-depth18-strength-plan.md)

## What actually failed

The earlier training was not literally worthless. Its frozen candidate passed quantization, WASM parity, and runtime checks, then scored 29–24–3 in fresh direct play. But its pair-accuracy gain over 22,890 validation positions was only `+0.000445`, or `+0.0445 percentage point`. That is not evidence of playing strength. The candidate missed the direct-play gate, so it will not be rescued with another epoch, another seed, or a changed threshold.

The schedule drift also needs a plain accounting. Ownership, tamper detection, keys, and PR boundaries were necessary for a reproducible formal run, but they did not directly increase strength. Too much work expanded those boundaries, while small training-metric changes were treated as if they were close to playing evidence. This lane reuses the safety foundation and spends most compute on new, stronger labels.

## A source assumption failed before compute started

The initial plan selected from 186,634 cycle-0 self-play positions. An independent audit counted the SFEN side to move and found `b=186,634 / w=0`. Four-ply sampling had retained only even plies. Using it would make the new fit, tune, and sealed sets one-sided, repeating a known parity defect. The pool was rejected before formal teacher computation began.

The replacement is an authenticated set of 800,000 positions from high-rated Floodgate Q1 2026 games: 14,861 games with `b=402,090 / w=397,910`. Every row has a YaneuraOu depth-12 score and final game outcome. That old score is selection metadata only, never a teacher target.

A read-only scan found 175,903 opening rows, 298,191 middle rows, and 197,394 late rows after the fixed ply and `|CP|≤1000` filters but before legal-count and overlap exclusions. Distinct-game supply is 11,392, 13,622, and 11,692 respectively, leaving ample headroom for one selected position per game. Formal quota feasibility is established only by the completed selection manifest after both exclusions.

| Phase   |    Ply | Eligible distinct games |        Fixed selection |
| ------- | -----: | ----------------------: | ---------------------: |
| Opening |  12–39 |                  11,392 | 2,048 (1,024 per side) |
| Middle  |  40–79 |                  13,622 | 3,072 (1,536 per side) |
| Late    | 80–119 |                  11,692 | 3,072 (1,536 per side) |
| Total   |        |                         | 8,192 (4,096 per side) |

Ranking is fixed by depth-12 CP/outcome surprise, then the lower player rating, then SHA-256. The selector takes one position per game and splits `game_id` before assigning fit 6,144, tune 1,024, and sealed 1,024. Every role is exactly 50/50 by side. It excludes every direct-teacher train/validation parent and child plus the initial position and six prefixes of all 56 selected v2/v4 formal openings that can be reconstructed from their seeds. The v4 candidate's exploratory 3–9 record in openings with exactly three central-file pawn pushes remains a hypothesis, not the sole selector criterion.

The source rows do not contain legal-move counts. The formal CLI rejects missing counts. A preprocessing step uses the production-rules move generator to add counts deterministically and binds the executing tool plus an 11-file move-rules closure by bytes/SHA from held, double-read descriptors. The selector reauthenticates that fixed closure and the current files, so a stale or fabricated self-asserted manifest cannot pass. It then binds the semantic-overlap inventory covering every direct parent/child and the 56 reconstructible selected formal openings.

Historical inventories retain only 3,198 v2 and 3,302 v4 fingerprints. They carry neither seeds nor SFENs and cannot be inverted into semantic positions. This plan does not claim to exclude that unrecoverable range. The limitation is fixed in the formal selection manifest; a wider scope would require recoverable source evidence and a new preregistered version.

The old source has another parity artifact after ply 120, where only `b` positions remain. The hard set therefore stops at ply 119. This does not remove endgames from training: the 200,944 direct training-replay rows retain broad late-game coverage, while a separate 22,890 rows remain out of training as preservation validation with late-game diagnostics. The fresh hard set has the narrower job of learning balanced sibling rankings in difficult opening and middlegame positions.

## What will be learned

Each parent is processed with the following fixed YaneuraOu configuration.

| Item                  |                                                             Fixed value |
| --------------------- | ----------------------------------------------------------------------: |
| Proposal              |                                                  MultiPV 12 at depth 16 |
| Extra moves           | Recorded move and a stable move that reaches depth 11 within 20 seconds |
| Final sibling rescore |                                                                Depth 18 |
| Processes             |                                                                      13 |
| Threads per process   |                                                                       1 |
| Hash per process      |                                                                 512 MiB |
| Timeout per parent    |                        600 seconds for YaneuraOu; 20 seconds for stable |

The stable move is only an auxiliary source for one extra candidate. If it does not reach depth 11 by its 20-second cooperative deadline, the partial move and score are discarded and an authenticated omission is recorded. That omission does not fail the parent: YaneuraOu MultiPV 12 and the recorded move are still always rescored at depth 18. The existing 200,944 direct training labels remain broad replay data; the separate 22,890 validation labels never enter training and remain reserved for preservation checks. Parent batches are 50% old direct replay and 50% new hard labels. The objective is 50% direct sigmoid BCE and 50% groupwise ListNet over sibling rankings.

Training restarts from the original alpha-050 checkpoint, not the failed v3/v4 candidate. The representation is HalfKP81, with one seed, exactly three epochs, and only the final epoch eligible. This prevents selecting a lucky-looking checkpoint after seeing results.

## What improvement is required

There is no guarantee that this will make the engine stronger. The advantage is narrower: it tests a materially different causal hypothesis and stops failures early.

Teacher authentication runs after 100 and 500 parents. Every parent must have at least two rows; any missing parent, non-depth-18 result, or technical fault is fatal. Both tune and untouched sealed sets must beat the initializer by at least `+2.0 percentage points` top-1 and `+1.0 point` sibling-pair accuracy. The old 22,890-row validation set must improve teacher MAE by at least `5 CP` without decreasing pair accuracy.

Before a teacher plan can be authorized, a second verifier double-reads the selection manifest and all 8,192 JSONL rows through held descriptors and recomputes canonical bytes/SHA/rows, semantic IDs, hardness and order, phase/side and role/side quotas, one position per game, zero cross-role game overlap, and the source/legal/overlap bindings. The mapping between compact selector phase names and preregistered phase names is explicit. The plan must bind both this evidence and the caller-authenticated merged source revision; an arbitrary 40-hex string is insufficient. The current teacher receipt validator still checks only the plan, counts, and path structure and grants no training-plan authority. That authority remains blocked until an artifact verifier reads each teacher JSONL through held descriptors and recomputes actual bytes, SHA-256, row counts, all 8,192 parent-role memberships, depth 18 for every target, and zero reuse of old depth-12 targets.

Only then do the integer and runtime gates run: zero int16 clipping, zero WASM mismatches, p99.9 quantization ratio `≤1.05`, absolute delta `≤300 CP`, and slowdown `≤5%`. Passing all of them may authorize a fresh 56-game screen with the unchanged requirement of 62/112 and zero technical faults. Any miss closes the lane. There is no extra epoch, seed, QAT, distillation, threshold change, or same-candidate continuation.

## Cost and current state

The prior depth-16 teacher run took about 11.47 hours for 24,000 parents. A linear 8,192-parent estimate is 3.91 hours, but depth 18 is materially more expensive, so the preregistered planning range is 8–16 hours for teacher generation. Legal-count enrichment and selection over 800,000 rows, training, and static gates should take under an hour; the 56-game screen should take 15–25 minutes. The total M4 Pro range is 9–18 hours, not a completion-time guarantee.

On July 30, 2026, PR #665—containing the preregistration, selector, and authenticators—was regularly merged as `0ec6807c7d6c13f3e7caf4f08d45e87ce1ba005b`. Against that merged revision, the formal run produced and reauthenticated a 243,368-row semantic-overlap inventory, an 800,000-row legal-count pool, and exactly 8,192 hard parents. The split is fit 6,144, tune 1,024, and sealed 1,024. All 8,192 `game_id` and `position_id` values are unique, cross-role game overlap is zero, and every role is 50/50 by side to move.

PR #666, which added the resumable runner and artifact verifier, was also regularly merged as `eaa03e570e1ed687c3479a38eba377807be4cd9e`. The stable assets were restored and reauthenticated, then v1 was started from clean `main` with an immutable 6,306-byte teacher plan whose SHA-256 is `c0b4a4ab2bc0a4b4a685b06e65afb0d3194551c72ae4382a9021754188a725b0`. The configured YaneuraOu `EvalDir`, however, was the parent `.../eval` directory rather than the actual `.../eval/eval` directory containing `nn.bin`. Every worker exited during USI initialization before the first parent. The terminal result is therefore `0 / 8,192` completed parents, zero depth-18 teacher rows, and one technical fault. This is an engine-path failure, not a strength or data result.

The v1 terminal-fault artifact remains preserved and closes the v1 output directory. The same family may not resume in place, and neither its `teacher-work.jsonl` nor its teacher plan may be overwritten. Its successor was the separate technical-recovery family `halfkp81-hard-depth18-engine-evaldir-v2`, which reused the exact authenticated 8,192-position selection at the same bytes/SHA but bound it to a new merged source revision and create-only output directory.

PR #667 passed all 15 checks and was regularly merged as `551759a171ac7fed5cf4a5b7cc2279dc60eea6bd`. v2 then started with the correct EvalDir, but terminally faulted after 49 / 8,192 parents and 585 rows when a stable-WASM depth-11 search reached the outer 600-second watchdog. Because one worker failure was broadcast to every active request in the shared pool, the parent named in the terminal fault did not by itself identify the cause. v2 was not resumed; the 13 unfinished parents in the first assigned set of 62 were isolated in a scratch-only diagnostic that cannot become training data.

Under the exact production limit, nine isolated searches reached depth 11 in 4.918–547.152 seconds, while four independent workers timed out at 600.000–600.003 seconds. Every child and pool closed cleanly and the formal work/fault artifacts remained unchanged. This proves multiple genuine depth-11 long tails rather than a single misattributed request or pool deadlock, with the pool-wide poison policy amplifying one worker failure into a run-wide stop. Merely increasing the watchdog still would not establish a completion bound.

The independent v3 family, `halfkp81-hard-depth18-bounded-stable-v3`, reuses the same 8,192-position selection by exact bytes/SHA but recomputes every parent from zero; none of v2's 49 parents or 585 rows may be reused. Stable-WASM contributes one move only when it completes depth 11 within 20 seconds. Any partial result is discarded, an ordinary deadline does not poison the pool, and only that worker is reaped and replaced. Formal launch also changed to a one-shot LaunchAgent with `KeepAlive=false` and `LaunchOnlyOnce=true`. These are not post-result strength-threshold changes. They define a new preregistered family in which a weak auxiliary candidate source cannot stop the stronger teacher indefinitely. The complete measured diagnosis is preserved in the [machine-readable memo](./data/shogi-halfkp81-depth18-v2-timeout-diagnostic-2026-07-30.json).

Implementation PR #668 passed all 15 checks and was regularly merged as `28a3310a9f16fafc4b192b090dcc3cdf2600de09`. From that clean `main`, the create-only v3 immutable teacher plan was published at 9,103 bytes with SHA-256 `6b69cb61044df051999191e6204be098205aa2301690f2a1becc0730baf9eda5`, then launched once as `com.meetyudai.shogi.halfkp81-depth18-bounded-v3-28a3310a`. The runner stopped before creating any worker with `bounded stable v3 preregistration is not canonical JSON with one terminal LF`. The tracked preregistration is pinned pretty-printed JSON, but the runner mistakenly imposed an additional canonical one-line JSON condition on those same bytes. The output namespace therefore contains only its teacher plan: 0 / 8,192 parents, zero teacher rows, and no YaneuraOu or stable worker ever started. The launchd job is unloaded. Training and strength evidence did not advance.

v3 will not be resumed or overwritten. Its v3r2 successor is a separate technical-recovery family bound to a new source revision, run fingerprint, and output namespace. The only contract change is accepting the pretty-printed tracked preregistration by its existing exact bytes/SHA without the contradictory one-line encoding requirement. The 8,192-position selection, fixed 20-second stable-WASM budget, required YaneuraOu depth-16 MultiPV 12, depth-18 rescore of every retained move, three epochs, one seed, and every strength gate remain unchanged. The v3 startup evidence and recovery boundary are preserved in a [machine-readable memo](./data/shogi-halfkp81-depth18-v3-startup-fault-2026-07-30.json).

PR #669, including v3r2, passed all 15 checks and was regularly merged as `f0ae74c208d3934849864c05cc68219f1834ef83`. The formal run completed 94 / 8,192 parents and 1,125 depth-18 teacher rows in five minutes and 11 seconds. Its optional stable path recorded 61 completed depth-11 proposals and 33 authenticated omissions at the 20-second cooperative deadline. Every omission discarded its partial result and reaped and replaced that worker, so the path that prevents a long search from failing its whole parent did operate. A subsequent worker startup nevertheless timed out and closed the family with one technical fault. The preserved plan is 10,827 bytes with SHA-256 `53e3aaabf2d08b9c440478e94dc1ccbdca58334618523dfcccf5d80ed3a072a9`; the 1,463,884-byte work ledger has SHA-256 `f182810f3629c6c57e81db759aca995eda4e4aa76787cd646a96295931856200`; and the 884-byte terminal fault has SHA-256 `10cfd2bc38fab128c3c7eff54bd9a5c005d96bb097ae24f6e322d4521bd4c1e6`. v3r2's 94 parents and 1,125 rows are bound to the failed family and may be used neither for training nor by its successor.

The same startup timeout reproduced in a stress wave. All 167 of 167 sampled stalled children were blocked in `readFileSync(3)`, waiting for EOF on file descriptor 3 carrying the worker source. The stop occurred before either WASM or its weights were loaded; it was not an FD leak, pool poison, the specific parent 49, or an overly short search timeout. v3r3 will use a new source revision, run fingerprint, and output namespace. Its parent will await `sourcePipe.end()` completion with a bound before beginning worker initialization. It does not extend a timeout, alter the teacher, training, or promotion contract, or reuse any v3r2 row. The formal run and diagnosis are fixed in a [machine-readable memo](./data/shogi-halfkp81-depth18-v3r2-worker-startup-fault-2026-07-30.json). This technical repair is not evidence of stronger play.

The post-fix preflight ran 13 CPU hogs and 13 × 512 MiB memory hogs, 6.5 GiB in total, while replacing 12 omitted workers in each of four waves. All 48 / 48 replacements completed in 20.63–20.79 seconds per wave with zero faults. All 60 telemetry records began initialization only after source transfer completed. Source transfer took 24.67–85.93 ms, averaging 41.05 ms; total startup took 149.26–344.17 ms, averaging 235.27 ms. Cleanup left zero of 60 workers, 13 CPU hogs, and 13 memory hogs alive. The receipt is 34,894 bytes with SHA-256 `8266fe89272f5888c35d94f65765a0a26edc8b95d12224cecae454f843b52580`. This is a stress PASS for the startup-ordering repair, not a strength result. The formal run has not started because its PR, CI, and regular merge prerequisites are not complete.

PR #670, including v3r3, was then regularly merged as `ae0ab195b2c552df6e3364f6278fb1217f067baa`, and the formal run started in a new namespace. It completed 162 / 8,192 parents and 1,948 depth-18 teacher rows: 103 stable proposals and 59 authenticated omissions. The durable 100-parent prefix passed with 1,198 rows and zero faults, and independent recomputation matched both its parent-ID and work-payload SHAs. A generic `bounded stable worker startup timeout` nevertheless terminated the run after the stable omission and worker replacement for selection parent 131. Selection parents 1–130 and 132–163 are durable, but the family may not resume or train, and none of its 1,948 rows may pass into a successor.

With no load, the same selection parent 131 produced an ordinary depth-10 omission in 20.103 seconds and its replacement worker started in 57.37 ms. The position therefore does not deterministically cause the startup fault. Formal work and logs, however, preserve no per-worker stage telemetry. They cannot establish whether this generic timeout stopped at fd 3 EOF, stdin/init, decode, or another startup wait. In particular, they do not prove that v3r2's confirmed fd 3 fault recurred unchanged.

Stable appeared as a source on 103 of the 1,948 formal rows, but only eight were unique stable-only additions unavailable from YaneuraOu MultiPV and the recorded move—0.41% of all rows. That small addition leaves a separate search, omission, and worker-replacement failure surface. The successor is therefore a new `halfkp81-hard-depth18-yaneura-only-v1` family that removes the entire mechanism. It deduplicates YaneuraOu depth-16 MultiPV 12 with the recorded move and rescores every retained candidate at depth 18. The 8,192-position selection, initializer, three epochs, one seed, and strength gates remain unchanged. There is no timeout extension and no reuse of v3r3 parents or teacher rows; a new source revision, run fingerprint, and output namespace start from zero. The v3r3 evidence and decision boundary are preserved in a [machine-readable memo](./data/shogi-halfkp81-depth18-v3r3-terminal-fault-2026-07-30.json).

PR #671, containing the YaneuraOu-only implementation, was regularly merged as `b75007ccdb202f76380d316feb0a7b3afd6b0e15`. Its formal plan is 11,854 bytes with SHA-256 `6168b156a0ff7411a0019e82f8cbe8ef2fa16c80610955aa7a97f7444bfe3e32`. A one-shot LaunchAgent started the 512-parent scratch preflight, but it stopped before selection authentication or engine startup with `teacher plan is not canonical JSON with one terminal LF`. Python's `json.dumps` preserved integer-valued floats such as `300.0`, while the TypeScript `canonicalJson` serialized the parsed value as `300`; the byte comparison therefore rejected two semantically equivalent JSON encodings. The result is 0 / 512 completed preflight parents, zero teacher rows, zero YaneuraOu starts, and zero formal teacher parents or rows. Training, games, and live weights also remain unchanged. This is a technical startup fault in the plan's numeric-encoding rule, not a strength result.

The existing v1 plan and output namespace remain preserved and may not be overwritten or resumed. Its `halfkp81-hard-depth18-yaneura-only-v1r2` successor will be a separate technical-recovery family bound to a new tracked preregistration, merged source revision, run fingerprint, and create-only namespace. The only change is making the Python and TypeScript canonical-number rules agree. The same authenticated 8,192-position selection, YaneuraOu depth-16 MultiPV 12 plus recorded move, depth-18 rescore of every candidate, initializer, three epochs, one seed, and all strength gates remain fixed. v1 contributes zero completed parents and zero teacher rows to v1r2. The startup evidence and recovery boundary are preserved in a [machine-readable memo](./data/shogi-halfkp81-depth18-yaneura-only-v1-startup-fault-2026-07-30.json).

PR #672, containing v1r2, was regularly merged as `20ec3fdf4ba81c89af4ba02bb7483b75945ebf4a`. Its formal plan is 15,649 bytes with SHA-256 `e351e6cf07e68ac7047a453b88695af476bc7211bfc75a71bece774e044694ab`, and the run fingerprint is `12e80cb27485ffa0f95dfa21e68117150652781c80c5dd54783cc3b89ca3ce3a`. The 512-parent scratch preflight passed plan and selection authentication, but `initializeWork` called `open(..., "wx")` for `teacher-work.jsonl` while the preflight output directory did not exist, so the run stopped with `ENOENT`. YaneuraOu never started; the result is 0 / 512 completed parents and zero teacher rows. Fault preservation then created the directory at mode `0700` and wrote, create-only, a 497-byte preflight fault with SHA-256 `13643215a0f16726625938196fa2cc4cdadfeef2fc06ae0f87070104ba5456e7` and an 823-byte teacher fault with SHA-256 `6d21199a482ccfdeb53cce1e45b2c608916c0e9cd4db74cf3aa18c8bf2a410bc`. The same v1r2 run may not resume, train, or contribute rows.

The next `halfkp81-hard-depth18-yaneura-only-v1r3` is a separate technical-recovery family. Its only authorized change is to prepare the preflight output directory create-only at mode `0700` before `initializeWork`. It must bind a new tracked preregistration, merged source revision, run fingerprint, and create-only namespace, and it inherits zero v1r2 parents and zero teacher rows. The authenticated 8,192-position selection, candidate policy, depth-16/depth-18 searches, 600-second timeout, initializer, three epochs, one seed, and every strength gate remain unchanged. All v1r2 artifacts and the recovery boundary are preserved in a [machine-readable memo](./data/shogi-halfkp81-depth18-yaneura-only-v1r2-preflight-fault-2026-07-30.json). This repair is not evidence of stronger play.

PR #673, containing v1r3, was regularly merged as `6b4c5d13f628f14ea1836a08f36d9ad9dd05e266`. The scratch preflight completed teacher computation for all 512 / 512 parents and produced 6,134 depth-18 rows with zero teacher faults: 384 fit parents and 4,605 rows, 64 tune parents and 771 rows, and 64 sealed parents and 758 rows. All 13 YaneuraOu processes quit, leaving zero active engines. The teacher receipt was structurally complete, but the subsequent validator stopped on `sha256:dc6230929aad5c53291c283200c456e2d178abcac1aec6537cf27aefcdb36a7f`. That position has six legal moves. The generator correctly requested MultiPV 6 via `Math.min(12, legalMoves.length)`, and its six candidates matched its six records, while the validator incorrectly required MultiPV 12 unconditionally. Across the 512 parents, requested MultiPV was 12 for 509 parents and 10, 6, and 2 for one parent each; candidate and record counts were aligned for every parent. This was not a teacher-search or timeout failure, but it occurred before artifact verification and training authority. v1r3 therefore may not resume, run formally, train, or contribute any of its 6,134 rows.

The successor `halfkp81-hard-depth18-yaneura-only-v1r4` will be a separate family bound to a new source revision, fingerprint, and create-only namespace. Its sole functional change is to make the validator check requested MultiPV against `min(12, legal move count)`. The same 8,192-position selection, YaneuraOu depth-16 candidate search and depth-18 rescore of every candidate, 600-second timeout, initializer, three epochs, one seed, and all strength gates remain fixed. It reuses zero v1r3 parents and zero v1r3 teacher rows. The measured artifacts and stop boundary are preserved in a [machine-readable memo](./data/shogi-halfkp81-depth18-yaneura-only-v1r3-preflight-fault-2026-07-30.json). Neither scratch teacher completion nor the validator repair is a strength claim.

PR #674, containing v1r4, was regularly merged as `28d113dbe6bff8ec7d21beb823058c8aae6c0bf4`. Its 512-parent preflight passed with 6,134 rows and zero faults, confirming that the v1r3 MultiPV-validator repair worked. The formal run also durably preserved 100 parents / 1,195 rows and 500 parents / 5,986 rows with zero faults, then reached 2,762 / 8,192 parents and 32,710 depth-18 teacher rows. Parent `sha256:5a0784bfa36f2961049c1eae3ca13fe041d089abad1228f9d935f48723826dae` subsequently hit the 600,000 ms parent-level timeout, closing the family with one technical fault. The 827-byte fault artifact has SHA-256 `6b4b4b476832dc905bbd6e665a2de9d6f9955b8cdf51c2d8b6208f81c54f284b`. After the stop, no runner or YaneuraOu process and no launchd job remained. The same family may not resume or train, and none of its 2,762 parents or 32,710 rows may be reused. This is not a strength result.

An isolated measurement of the fault parent took 56.274 seconds and 78,043,847 nodes for the depth-16 proposal, 423.887 seconds and 513,257,095 nodes for the twelve independent depth-18 rescores, and 480.161 seconds and 591,300,942 nodes in total. The `7c6d` and `7c8c` rescores alone took 217.877 and 118.089 seconds. The parent fits under 600 seconds in isolation, but under the formal 13-process setting, multiple legitimate long-tailed searches ran concurrently, saturated shared compute, and pushed parent wall time past the limit. The independent v1r5 family therefore fixes the process count from 13 to four, removes the old aggregate parent deadline, and applies a 3,600-second timeout to each USI search—the depth-16 proposal and every depth-18 rescore. It publishes a ledger entry only after the whole parent succeeds; any individual search timeout closes the family with a terminal fault. A sentinel preflight of the fault parent is also mandatory before formal execution. v1r5 starts from a new source revision, fingerprint, and create-only namespace and reuses zero v1r4 parents or rows. The selection, teacher candidates, depth-16/depth-18 searches, initializer, three epochs, one seed, and every strength gate remain unchanged. Exact measurements and the preregistered boundary are preserved in a [machine-readable memo](./data/shogi-halfkp81-depth18-yaneura-only-v1r4-formal-terminal-fault-2026-07-31.json).

PR #677, containing v1r5, was regularly merged as `3b3f85cc7ead5eeda637a76a63d07b98e10aa1c4`. The scratch preflight that replicated v1r4's long-tailed parent four ways completed all four replicas in 614.975–615.744 seconds with zero faults. The formal run reached 776 / 8,192 parents and 9,301 depth-18 teacher rows, but the persistent engine reset for reported selection #775, parent `sha256:2253ddad6fd346433f7a98962235105fae9f431a5487be32c6e27f8c5afaa9fa`, ended with `USI timeout after 120000ms`, closing the family with one technical fault. The work ledger is 11,524,585 bytes with SHA-256 `c8414f6669e53115c7d321638e9f61c1d3ce46a1c80e3076029faf47a7459bba`; the 851-byte fault has SHA-256 `e4327ed8b5552199a564084ea117edf9804272ed43894f25b09b13bb5f5e6002`. v1r5 may not resume or train, none of its 776 parents or 9,301 rows may be reused, and live state remains unchanged.

With a fresh engine, the same fault parent initialized in 364 ms, completed 13 resets in 118–227 ms each, completed the depth-16 proposal in 2,104 ms, finished all twelve depth-18 rescores, and finished the entire parent in 10,950 ms. The cause was therefore not a long-tailed position or the 3,600-second search timeout, but a transient liveness failure while resetting a persistent engine between parents. The independent v1r6 family preserves the same 8,192-position selection, teacher, depth-16/depth-18 searches, four processes, 3,600-second per-USI-search timeout, training, and strength gates. Only the first reset timeout may trigger recovery: terminate and reap that engine, replace it, and recompute the entire unpublished parent once from the depth-16 proposal. No partial result from the first attempt may be reused, and exactly one parent is published only after complete retry success. Search timeouts, non-reset faults, and a second reset timeout receive no retry and close the family. A read-only audit found zero mismatches across v1r5's 776 durable parent records, but a safe importer that rebinds them to the new fingerprint, schema, and digests would take four to eight hours to implement and verify while avoiding only about six hours of recomputation. v1r6 therefore adds no such complexity: it starts from a new source revision, fingerprint, and create-only namespace and recomputes from zero parents and zero rows. Fault evidence is preserved in the [memo](./data/shogi-halfkp81-depth18-yaneura-only-v1r5-formal-terminal-fault-2026-07-31.json), and the fixed contract is separated into the [v1r6 preregistration](./data/shogi-halfkp81-depth18-yaneura-only-v1r6-preregistration-2026-07-31.json).

v1r6 reached 3,262 / 8,192 parents and 38,393 depth-18 teacher rows before the independent `G*6a` rescore for parent `sha256:622377e74345bfcbe509b903ae89e37dfec48e493db0331780b5423382d926a1` timed out after 3,600,000 ms, formally closing the family with one technical fault. Isolated runs of the same move also hit wall timeouts at depth 16 after 600,000 ms, depth 17 after 1,800,000 ms, and depth 18 after 3,600,000 ms. This is therefore a genuine search tail rather than another reset or process deadlock. v1r6 may not resume, and v1r7 reuses none of its 3,262 parents, 38,393 rows, or partial parent state. This is a teacher-generation technical result, not evidence of stronger play.

The raw node-ladder transcript shows that a 100-million-node cap ended the search in 57.901 seconds. The cap-reaching `depth 16 / nodes 100,000,012` line, however, has the same score and PV as the preceding `depth 15 / nodes 95,168,959 / cp -3360` line and is treated as final telemetry from an incomplete iteration. Applying the same conservative rule leaves depth 13 at both five million and twenty million nodes, and depth 11 at one million. The independent v1r7 family leaves the depth-16 MultiPV 12 proposal uncapped and gives only each independent MultiPV 1 rescore the uniform `depth 18 OR 100,000,000 nodes` limit. A dual-limit call must explicitly supply minimum completed depth 15; omission is rejected and there is no default. When the cap wins, it selects the last line strictly below the cap, requires completed depth 15 or greater, and also requires a later same-rank cap witness whose depth exceeds the selected depth. The cap line supplies no score; the ledger records `cap_witness_depth`, `cap_witness_nodes`, `selected_precedes_witness`, and `completed_iteration_witness_depth`. Selected-line nodes and the actual maximum nodes searched use separate fields and separate totals and cannot substitute for each other. No candidate is dropped. At most 32 parents may cap—24 fit, four tune, and four sealed—and at most 64 teacher moves may cap—48 fit, eight tune, and eight sealed—with no more than two per parent. Formal generation checks these budgets after every completed candidate and fails closed immediately; artifact verification independently recounts every budget from the final ledger. Exceeding a budget, falling below depth 15, missing the ordered witness, or having only a partial result closes the family. In v1r6's successful ledger, ten moves across eight parents exceeded 100 million nodes. A simple projection to 8,192 parents is about 25 moves across 20 parents; that motivates the preregistered headroom but is not a guarantee. Because a capped depth-15-or-greater result can change the exact depth-18 score's sign, within-parent rank, top move, or magnitude and can introduce mixed-depth label bias, this is an explicit change to the strength contract.

To use more of the M4 Pro's ten performance and four efficiency cores, v1r7 fixes eight persistent engines at one thread and 512 MiB of hash each. Before opening the ready PR, all ten v1r6 searches above 100 million nodes across eight parents must be rerun under the new dual contract in scratch and compared with the preserved exact depth-18 results. All ten must select depth 15 or greater, with zero score-sign flips, zero parent top-move changes, at most one place of within-parent rank movement for each capped move, and an absolute CP delta no greater than 300. A miss stops v1r7 without changing a threshold. Before formal generation, a pathological sentinel must then complete eight concurrent replicas of the known tail parent, and a 512-parent formal-like preflight must complete and verify under the exact formal contract; both are scratch-only. The depth-16 MultiPV 12 plus recorded-move proposal, retention of every candidate, authenticated 8,192-position selection, initializer, three epochs, one seed, and every downstream numeric threshold remain unchanged. Tune and sealed must each pass the same thresholds twice: once on all parents and once on uncapped parents only, separating any apparent improvement caused by capped rows. Formal execution is forbidden before a ready PR, all required CI, a regular merge, and authentication of a new clean main revision. The measured fault is preserved in a [machine-readable memo](./data/shogi-halfkp81-depth18-yaneura-only-v1r6-formal-terminal-fault-2026-08-01.json), and the next family's fixed boundary is in the [v1r7 preregistration](./data/shogi-halfkp81-depth18-yaneura-only-v1r7-preregistration-2026-08-01.json).

That pre-PR calibration formally failed. All ten moves selected depth 15 or greater with zero sign flips and zero parent top-move changes, but within-parent rank displacement reached four against the limit of one, and absolute CP delta reached 33,989 against the limit of 300. The worst case was `2a1c` in parent `sha256:b70979181f66fc8960a5817a271f9422d659f0cac1213c431ef9d76bd8a25726`: its exact depth-18 score of -35,281 became -1,292 at capped depth 15, and its rank moved from twelfth to eighth. The 7,391-byte receipt has SHA-256 `118d6cd1cbd8abf1beab89e7ce88e09a2f154f80503b22dda944bb677f8d11b9`. Although 100 million nodes bounded runtime, it did not preserve teacher-label fidelity. As preregistered, v1r7 therefore stopped before a ready PR without relaxing a threshold. The sentinel, 512-parent preflight, formal teacher, training, games, and live changes will not start. A two-billion-node `G*6a` scratch diagnostic was run as evidence for the next decision, but no successor family or preregistration was created before that result. Exact measurements and the stop boundary are preserved in the [v1r7 calibration failure memo](./data/shogi-halfkp81-depth18-yaneura-only-v1r7-pre-pr-calibration-failure-2026-08-01.json).

That two-billion-node `G*6a` scratch diagnostic also failed to establish a depth-16 floor. It ran for 1,924,243 ms and reached 2,000,000,532 nodes, but the last acceptable completed result remained depth 15 at 95,168,959 nodes and cp -3360. Depth 16 first appeared at 402,219,590 nodes and remained an `upperbound` through the two-billion-node termination, with hashfull at 1000; it never became an exact completed result. The 995-byte receipt has SHA-256 `90551c3a1c543d43afce2e58ff9324a66ddb8a2d608088cb4203837d6b8a7c7c`. A prospective v1r8 that raised the cap to two billion while requiring depth 16 or greater was therefore closed as a diagnostic failure before preregistration. Before creating v1r9, the next bounded investigation increased Hash to 2, 4, and 8 GiB and ran exact depth 18 to isolate the effect of transposition-table saturation. Evidence is preserved in the [v1r8 floor-16 failure memo](./data/shogi-halfkp81-depth18-yaneura-only-v1r8-floor16-diagnostic-failure-2026-08-01.json).

Running the same `G*6a` to exact depth 18 with more Hash completed at 2 GiB in 280,702 ms and 456,368,061 nodes with cp -3325 and maximum hashfull 796; at 4 GiB in 214,189 ms and 370,179,475 nodes with cp -35281 and hashfull 368; and at 8 GiB in 256,113 ms and 433,851,102 nodes with cp -35281 and hashfull 229. Their receipts are respectively 932 bytes / SHA-256 `0047c046c49ddb361c716daf722b94c19bb1465446b51104b54ee02daded7556`, 913 bytes / `fe0699735b39f47189ebd183677199785c822726f584ed06231b942c10d015a2`, and 864 bytes / `5e334cb4334fe079f2ee58b29687f7748ad65ffa8e63fcd6be902565d4e0fd58`. Two independent 8-GiB replicas then completed in 251,029 and 251,141 ms. Across the initial run and both replicas, cp -35281, 433,851,102 nodes, and the 17-move PV were identical. The replica receipts are 886 bytes each with SHA-256 `f8b8b8b9627cd549742bd3a5b5d040e9882b1a29a4de3256c3dc6db89d2adac3` and `0c4f90b692d2634e10fe52b8d8cd2aa5bbd3948b18461d4bd460386cc76157c2`. Although the 4-GiB and 8-GiB probes returned the same cp here, their node counts and PVs differed, so they are not the same teacher function. The fallback is therefore fixed at Hash 8 GiB; downgrades or substitutions to Hash 4 GiB or 2 GiB are forbidden. This is operational evidence that large Hash can complete the known tail at exact depth 18, not evidence of stronger play.

The independent v1r9 family handles ordinary parents with eight one-thread, Hash-512-MiB processes and first fixes the candidate set and order from depth-16 MultiPV 12 plus the recorded move. Each candidate is rescored with `depth 18 OR 2B nodes`. The normal dual search's `minimum_completed_depth=1` exists only to record reaching the cap as a valid routing result; it is not the teacher-label quality floor. A parent publishes normally only if every candidate reaches exact depth 18. If any move reaches the 2B cap first, its score is never a teacher label: every normal-lane rescore for that parent is discarded, and the fixed candidate set enters a FIFO fallback. The fallback runs at most two processes, starts a fresh Hash-8-GiB engine for each parent, and recomputes every candidate from zero to uncapped exact depth 18. A whole parent publishes only after every candidate succeeds. Candidate omission, reuse of normal partials, reuse of old-family rows, and capped labels are all zero. Each search has a four-hour timeout; only a reset timeout may replace the engine and recompute the entire parent once.

The fallback limits are eight parents overall—six fit, one tune, and one sealed—and 104 exact fallback searches—78 fit, 13 tune, and 13 sealed—with at most 13 per parent. Exceeding a limit closes the family immediately. Runtime and the artifact verifier independently recount actual cap parents, trigger searches, fallback rows by role, and zero capped labels. Before formal generation, candidate-order, the ten preserved >100M searches, known-`G*6a` routing, mixed load with eight normal and two fallback engines, and a 512-parent formal-like preflight must all pass. The 8,192-position selection, initializer, three epochs, one seed, and downstream gates remain unchanged, and v1r6 through v1r8 contribute zero parents and rows. Every execution authority remains false until a ready PR, all required CI, a regular merge, clean-main authentication, and the artifact-verifier implementation tests pass. After the formal teacher finishes, a separate independent artifact-verifier pass over the generated formal artifacts is required before training; it is not the same pre-execution implementation-test gate. The fixed contract is preserved in the [v1r9 preregistration](./data/shogi-halfkp81-depth18-yaneura-only-v1r9-preregistration-2026-08-01.json).

PR #679 passed all 15 checks and was regularly merged as `141e4e39e79c1772a18dc79dbb4bbf480bfe4a53`. Immediately after create-only publication from clean main, v1r9 runtime-plan self-authentication stopped with `Yaneura-only v1r9 fixed authority differs`. The tracked preregistration schema contains `hard-depth18`, while the sealed runtime expectation omitted only `hard`. No YaneuraOu process, formal parent, or teacher row started. The written 16,406-byte plan with SHA-256 `c42b47efa41293f8170fe3792bcfb21798b9dd287226c67face6eb49e163ea19` and the v1r9 namespace will not be deleted or reused. The successor v1r10 preserves the same selection, teacher, Hash-routing contract, training, and strength gates while requiring a matching schema identity and new tracked preregistration, merged source revision, fingerprint, and create-only namespace. The boundary is preserved in the [machine-readable memo](./data/shogi-halfkp81-depth18-yaneura-only-v1r9-runtime-plan-publication-failure-2026-08-01.json).

The live baseline remains `public/shogi-nnue-weights.bin`, 1,185,988 bytes, SHA-256 `e4e738f99fbd8685bcfe2700e4df364af6274e75b44b298432fc313b9a3e28dc`. Completing selection is not evidence of stronger play, and the public flag remains unchanged.

The exact conditions and measured feasibility data are separated into the [machine-readable memo](./data/shogi-halfkp81-hard-depth18-strength-plan-2026-07-29.json).
