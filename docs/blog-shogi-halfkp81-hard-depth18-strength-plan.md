# 将棋AIを次に強くする実験：片側だけの自己対局を捨て、高レート棋譜の難局面をdepth18で学び直す

> HalfKP81 v4はfresh 56局で29勝24敗3分、61/112だった。点推定では上向いたが、事前合格線62/112に1点届かず、不採用にした。次は同じ候補の救済ではない。既存の広い教師データを保ちつつ、高レート棋譜80万局面から難しい8,192局面を選び、その兄弟手をやねうら王depth18で新しく採点する独立実験にする。[English](./blog-shogi-halfkp81-hard-depth18-strength-plan.en.md)

## まず、何が失敗したのか

これまでの学習は完全に無意味だったわけではない。凍結候補は量子化・WASM一致・実行速度の検査を通り、fresh対局でも29勝24敗3分だった。しかし、22,890 validation局面でのpair accuracy改善は`+0.000445`、つまり`+0.0445 percentage point`にすぎない。これは棋力向上の証明ではない。56局でも合格線に届かなかったので、同じ候補を追加epoch、別seed、閾値変更で救済しない。

時間を使いすぎた原因も明確にする。所有権、改ざん検知、鍵、PR境界は再現可能な正式実験には必要だったが、棋力を直接上げる作業ではない。その境界作りを必要以上に広げ、学習指標の小幅改善を実戦の強さに近いものとして扱った。今回は、すでにある安全基盤を再利用し、計算時間の大半を新しい強い教師ラベルへ振る。

## 使う直前に、自己対局poolの前提ミスを見つけた

当初は自己対局cycle0の186,634局面から選ぶ計画だった。独立監査でSFEN手番を全数集計すると、`b=186,634 / w=0`だった。4手間隔samplingが偶数plyだけを残したparity defectである。このままなら新しいfit、tune、sealedも全て同じ手番になり、既知の偏りをもう一度学習する。正式教師計算を始める前にこの入力を破棄した。

代わりに使うのは、Floodgate Q1 2026の高レート棋譜から作った認証済み800,000局面である。14,861対局、手番は`b=402,090 / w=397,910`。全rowはやねうら王depth12と最終勝敗を持つ。旧depth12 CPは難しさの選抜にだけ使い、教師値としては使わない。

read-only走査では、合法手数付与と重複除外より前の供給として、対象plyかつ`|CP|≤1000`の局面はopening 175,903、middle 298,191、late 197,394だった。異なる対局数も11,392、13,622、11,692あり、「1対局から1局面」の8,192親に十分な余裕がある。正式quotaの成立は、合法手数付与と重複除外を完了した選抜manifestで初めて確定する。

| phase   |    ply | eligible distinct games |          固定選抜数 |
| ------- | -----: | ----------------------: | ------------------: |
| opening |  12–39 |                  11,392 | 2,048（b/w各1,024） |
| middle  |  40–79 |                  13,622 | 3,072（b/w各1,536） |
| late    | 80–119 |                  11,692 | 3,072（b/w各1,536） |
| 合計    |        |                         | 8,192（b/w各4,096） |

選抜は、depth12 CPと最終勝敗の食い違い、両対局者の低い方のrating、最後にSHA-256の順で決定する。1対局から1局面とし、`game_id`を分けてからfit 6,144、tune 1,024、sealed 1,024へ割り当てる。各roleもb/wを完全に50/50にする。既存の直接教師train/validationと、seedから再構成できるv2/v4の選択済み正式opening 56件（初期局面＋各6手のprefix）のsemantic positionを除外する。中央筋の歩を多く突いたopeningでv4候補が3勝9敗だった所見は探索的仮説に留め、唯一の選抜条件にはしない。

元データには合法手数がない。そのまま正式選抜すると「強制手を除外した」と証明できないため、正式CLIは合法手数の欠落を拒否する。SFENから本番ルールの合法手生成器で合法手数を決定的に付与し、実行toolと手生成rules closure 11ファイルのbytes/SHAをheld descriptorで二重読取してmanifestへ固定する。selectorもその固定closureと実ファイルを再認証するため、自己申告だけの古い・偽造manifestは通らない。さらに、直接教師の親子全局面と再構成可能な選択済み正式56序盤を束ねたsemantic-overlap inventoryを結合する。両方が揃うまで、選抜結果を正式成果物として扱わない。

過去inventoryにはv2 3,198件、v4 3,302件のfingerprintしか残っておらず、seedやSFENがないためsemantic positionへ逆変換できない。この範囲まで「完全除外した」とは主張しない。正式選抜manifestにこの限界を固定し、復元可能な元資料が見つかった場合だけ別の事前登録版で範囲を広げる。

ply 120以降の旧poolは別のsampling都合でbしか残っていなかったため、新しいhard setには使わない。endgameを捨てるという意味ではない。既存200,944件の学習用direct replayには終盤が残り、別の22,890件は学習に混ぜないpreservation validationとして終盤を含む診断に使う。新hard setの目的は「両手番が揃う難しい序中盤の兄弟順位」へ限定する。

## 新しく学ぶもの

各親局面で、やねうら王を次の固定条件で動かす。

| 項目               |                                      固定値 |
| ------------------ | ------------------------------------------: |
| proposal           |                         MultiPV 12、depth16 |
| 追加する手         | 棋譜の手、20秒内にdepth11まで完了した安定手 |
| 全兄弟手の最終採点 |                                     depth18 |
| process            |                                          13 |
| thread / process   |                                           1 |
| hash / process     |                                     512 MiB |
| timeout / parent   |               やねうら王600秒、安定手は20秒 |

安定手は候補を一つ増やす補助経路に限定する。20秒のcooperative deadlineまでにdepth11へ届かなければ途中の手・scoreは捨て、認証済みの「省略」として記録する。これは親全体の失敗ではなく、やねうら王のMultiPV 12と棋譜の手は必ずdepth18で再評価する。元の学習用200,944 direct labelは捨てず、広い局面を忘れないためのreplayにする。22,890 validation labelは学習に混ぜず、保存性能の検査専用に保つ。学習batchは親単位で旧direct 50%、新hard 50%。目的関数はdirect sigmoid BCE 50%と、同じ親の兄弟手順位を学ぶListNet 50%に固定する。

initializerは失敗したv3/v4候補ではなく、元のalpha-050 checkpointへ戻す。HalfKP81、1 seed、3 epochs、最終epochだけを候補にする。うまく見えるcheckpointを後から選ばない。

学習入口を実装する段階で、既存trainerをそのまま使えない差も判明した。`ml/train.py`の兄弟手ListNet経路はdirect項がsigmoid後のMSEで、固定したsigmoid BCEではない。一方、direct-teacher v2 trainerはBCEを持つが、ListNetと旧direct / 新hardの親単位50 / 50混合を持たない。さらに現在の事前登録は「seedは1本」と3 epochsまでは固定するが、seed値、batch、learning rate、optimizer、deviceを固定していない。この状態でどちらかを近似的に起動すると、結果を見る前に決めるべき実験条件をlauncherが勝手に補うことになる。採用した方針Aでは、verified artifact receipt、全authority chain、fit 6,144 / tune 1,024 / sealed 1,024、元initializerとdirect replayを実bytes / SHAで再認証するcreate-only handoffまでをこのPRに含め、optimizer権限はfalseのままにする。不足条件と完全互換のBCE / ListNet trainerは別のprospective PRでformal training前に固定する。それまでは学習回数0、live weight変更0である。

## どこまで改善すれば次へ進むか

強くなる保証はない。あるのは、以前と違う原因を狙い、失敗を安く切る設計である。

最初に教師100親、次に500親でdepth18、親ごと最低2手、出力完全性、fault 0を認証する。学習後はtuneと未使用sealedの両方でinitializer比top-1 `+2.0 percentage points`以上、兄弟pair `+1.0 point`以上を必須にする。旧22,890 validationでもteacher MAE `5 CP`以上改善、pair差非悪化を求める。

teacher planを作る前にもselection manifestと8,192行JSONLをheld descriptorで二重読取し、canonical bytes/SHA/row数、semantic ID、hardness、順序、phase/sideとrole/side quota、1対局1局面、role間game重複0、source/legal/overlap bindingを再計算する。短いphase名と事前登録名の対応も固定した。planは任意の40桁文字列ではなく、呼出側が認証したmerged source revisionとこのselection evidenceの両方へ結合する。現在のreceipt validatorは計画・件数・pathの構造だけを検査し、学習開始権限を持たない。教師JSONLをheld descriptorで読み、実bytes/SHA/row数、8,192親のrole所属、全targetのdepth18、旧depth12 target混入0を再計算するartifact verifierが通った後にだけ、学習計画を作れるようにする。

その後にint16 clipping 0、WASM mismatch 0、p99.9量子化比`≤1.05`、絶対差`≤300 CP`、実行速度低下`≤5%`を通す。全部通った場合だけfresh 56局へ進み、以前と同じ62/112以上、technical fault 0を要求する。どれか一つでも失敗したら終了する。追加epoch、追加seed、QAT、蒸留、閾値変更、同じ候補の続行はしない。

## 時間と現在地

過去の実測では24,000親のdepth16教師が約11.47時間だった。8,192親なら単純換算約3.91時間だが、今回はdepth18なので保守的に8〜16時間を見込む。80万局面の合法手付与・選抜、学習、static gateは1時間未満、56局screenは15〜25分が目安で、合計9〜18時間である。これはM4 Pro上の範囲予測で、保証時刻ではない。

2026年7月30日、事前登録・選抜器・認証器を含むPR #665は通常mergeされた（merge `0ec6807c7d6c13f3e7caf4f08d45e87ce1ba005b`）。そのmerged revisionでsemantic-overlap 243,368行、合法手数付きpool 800,000行、hard parent 8,192行を正式生成・再認証した。選抜はfit 6,144、tune 1,024、sealed 1,024で、8,192件の`game_id`と`position_id`は全て一意、role間game重複は0、各roleの手番は50/50だった。

再開可能runnerとartifact verifierを含むPR #666も通常mergeされた（merge `eaa03e570e1ed687c3479a38eba377807be4cd9e`）。stable assetsを復元・再認証し、clean mainから6,306 bytes、SHA-256 `c0b4a4ab2bc0a4b4a685b06e65afb0d3194551c72ae4382a9021754188a725b0`のimmutable teacher planを発行してv1を実行した。しかし、やねうら王へ渡した`EvalDir`が実際の`nn.bin`を含む`.../eval/eval`ではなく、その1階層上の`.../eval`だった。workerは最初の親を処理する前のUSI初期化で終了したため、結果は完了`0 / 8,192`親、depth18教師`0`行、technical fault 1である。これは棋力やデータの失敗ではなく、engine pathの技術的失敗である。

v1のterminal faultは消さずに保存し、v1 output directoryを閉じた。同じfamilyをその場で再開する権限はなく、既存の`teacher-work.jsonl`やteacher planを上書きもしない。次のv2は`halfkp81-hard-depth18-engine-evaldir-v2`という別のtechnical-recovery familyにした。認証済み8,192選抜は同じbytes/SHAのまま再利用する一方、v2修正を含む新しいmerged source revisionと、v1とは別のcreate-only output directoryへ結合した。

PR #667は全15 check成功後に通常mergeされ（merge `551759a171ac7fed5cf4a5b7cc2279dc60eea6bd`）、v2は正しいEvalDirで開始した。しかし49 / 8,192親、585行まで進んだ所で、stable-WASM depth11探索が600秒の外側watchdogへ到達してterminal faultになった。共有poolは1 workerの失敗を全active requestへ配ったため、faultに書かれた親だけが原因とは断定できなかった。v2は再開せず、最初の62件で未完だった13親をtrainingに使わないscratch診断へ分離した。

隔離したexact production条件では9件が4.918〜547.152秒でdepth11へ到達し、4件はそれぞれ別workerで600.000〜600.003秒のtimeoutを再現した。全childとpoolは正常に終了し、formal work/faultも不変だった。したがって原因は単一の誤帰属やpool deadlockではなく、複数局面の本物のdepth11長尾と、それを全pool停止へ拡大する実装だった。timeoutをさらに延ばすだけでは完走上限を作れない。

独立v3 `halfkp81-hard-depth18-bounded-stable-v3`は、同じ8,192選抜をbytes/SHA固定で再利用するが、v2の49親・585行は一切流用せず0から計算する。stable-WASMは20秒以内にdepth11へ完了した場合だけ候補手を一つ足し、未完partialは捨てる。通常deadlineはpool全体をpoisonせず、そのworkerだけを回収・交換する。正式起動も`KeepAlive=false`、`LaunchOnlyOnce=true`のone-shot LaunchAgentに変えた。これらは結果を見て合格線を変える操作ではなく、弱い補助候補が強い教師生成全体を無期限停止させないための新しい事前登録familyである。診断の全実数は[機械可読メモ](./data/shogi-halfkp81-depth18-v2-timeout-diagnostic-2026-07-30.json)に保存した。

実装PR #668は全15 check成功後に通常mergeされた（merge `28a3310a9f16fafc4b192b090dcc3cdf2600de09`）。clean mainからv3のimmutable teacher plan（9,103 bytes、SHA-256 `6b69cb61044df051999191e6204be098205aa2301690f2a1becc0730baf9eda5`）をcreate-onlyで発行し、`com.meetyudai.shogi.halfkp81-depth18-bounded-v3-28a3310a`としてone-shot起動した。しかしrunnerはworkerを作る前に`bounded stable v3 preregistration is not canonical JSON with one terminal LF`で停止した。tracked preregistrationは固定済みのpretty JSONなのに、runnerが同じbytesへcanonical 1行JSONという追加条件を誤って課していたのが原因である。出力namespaceに残ったのはteacher planだけで、完了は0 / 8,192親・0教師行、やねうら王とstable workerの起動も0だった。launchd jobはunload済みで、学習・棋力証明は進んでいない。

v3のplanやnamespaceを上書きして再開はしない。修正版v3r2は新しいsource revision、run fingerprint、output namespaceへ結合する別のtechnical-recovery familyにする。変更するのは、pretty JSONのtracked preregistrationを既存のexact bytes/SHAで認証し、矛盾する1行化条件を外す点だけである。8,192選抜、stable-WASMの固定20秒上限、やねうら王depth16 MultiPV 12、全候補のdepth18再評価、3 epoch・1 seed、全棋力gateは変えない。v3 startup faultの証拠と境界は[機械可読メモ](./data/shogi-halfkp81-depth18-v3-startup-fault-2026-07-30.json)に保存した。

v3r2を含むPR #669は全15 check成功後に通常mergeされた（merge `f0ae74c208d3934849864c05cc68219f1834ef83`）。正式実行は5分11秒で94 / 8,192親、depth18教師1,125行まで進んだ。optional stable候補はdepth11完了proposal 61件と、20秒のcooperative deadlineによる認証済みomission 33件だった。omissionは全てpartialを捨て、そのworkerを回収・交換しており、長尾探索を親全体の失敗にしない経路そのものは動作した。しかし次のworker startupがtimeoutし、technical fault 1でfamilyを終了した。planは10,827 bytes、SHA-256 `53e3aaabf2d08b9c440478e94dc1ccbdca58334618523dfcccf5d80ed3a072a9`、workは1,463,884 bytes、SHA-256 `f182810f3629c6c57e81db759aca995eda4e4aa76787cd646a96295931856200`、terminal faultは884 bytes、SHA-256 `10cfd2bc38fab128c3c7eff54bd9a5c005d96bb097ae24f6e322d4521bd4c1e6`で固定した。v3r2の94親・1,125行は失敗familyに結合済みなので、学習にも次familyにも流用しない。

同じstartup timeoutはstress waveで再現した。停止childをsampleした167 / 167件が、worker sourceを渡すfd 3の`readFileSync(3)`でEOFを待っていた。停止点はWASMやweightsを読む前であり、FD leak、pool poison、特定のparent 49、探索timeoutの長さは原因ではなかった。v3r3は新しいsource revision、run fingerprint、output namespaceを使い、親が`sourcePipe.end()`の完了を上限付きで待ってからworker initへ進むようにする。timeoutの延長、教師・学習・合格線の変更、v3r2行の再利用はしない。実行・診断の全実数は[機械可読メモ](./data/shogi-halfkp81-depth18-v3r2-worker-startup-fault-2026-07-30.json)に保存した。この修正もまだ棋力向上の証拠ではない。

修正後preflightでは、13 CPU hogと13 × 512 MiB（合計6.5 GiB）のmemory hogを同時に置き、12 workerのomission・交換を4 wave、合計48 / 48件実行した。各waveは20.63〜20.79秒、fault 0だった。telemetry 60件は全てsource転送完了後にinitを開始し、source転送は24.67〜85.93 ms（平均41.05 ms）、startup全体は149.26〜344.17 ms（平均235.27 ms）だった。終了後はworker 60、CPU hog 13、memory hog 13のaliveが全て0になった。receiptは34,894 bytes、SHA-256 `8266fe89272f5888c35d94f65765a0a26edc8b95d12224cecae454f843b52580`である。これは起動順序修正のstress PASSであり、正式runはPR・CI・通常merge前なのでまだ開始していない。

その後、v3r3を含むPR #670を通常mergeし（merge `ae0ab195b2c552df6e3364f6278fb1217f067baa`）、新しいnamespaceで正式実行した。結果は162 / 8,192親、depth18教師1,948行、stable proposal 103件、認証済みomission 59件である。prefix 100は1,198行、fault 0でdurable PASSし、parent IDとwork payloadのSHAも再計算一致した。しかしselection #131のstable omission・worker交換後に、同じgenericな`bounded stable worker startup timeout`が発生しterminal終了した。selection #1〜130と#132〜163はdurableだが、同family再開・学習は許可されず、1,948行を次familyへ流用もしない。

同じ#131 parentを無負荷で隔離すると、depth10で20.103秒の正常omissionになり、交換workerも57.37 msで起動した。したがってこの局面自体が必ずstartup faultを起こすわけではない。一方、正式work/logにはworkerごとのstage telemetryが残っていないため、今回のgeneric timeoutがfd 3のEOF、stdin/init、decode、または別のstartup待ちのどこだったかは証明できない。v3r2で確定したfd 3原因がそのまま再発した、とも断定しない。

正式1,948行のうちstableがsourceとして現れたのは103行だが、やねうら王MultiPVと棋譜の手では得られないstable-onlyの一意な追加は8行、全体の0.41%だけだった。この小さい追加価値に対し、stable候補経路は探索・omission・worker交換という独立した故障面を残す。次は新しい`halfkp81-hard-depth18-yaneura-only-v1`とし、この機構全体を外す。やねうら王depth16 MultiPV 12と棋譜の手をdeduplicateし、全候補をdepth18で再評価する契約、8,192選抜、initializer、3 epoch・1 seed、棋力gateは変えない。timeout延長もせず、v3r3の親・教師行を再利用せず、新しいsource revision、run fingerprint、output namespaceで0から計算する。v3r3の証拠と判断境界は[機械可読メモ](./data/shogi-halfkp81-depth18-v3r3-terminal-fault-2026-07-30.json)に保存した。

やねうら王専用実装を含むPR #671は通常mergeされた（merge `b75007ccdb202f76380d316feb0a7b3afd6b0e15`）。そのrevisionに結合したformal planは11,854 bytes、SHA-256 `6168b156a0ff7411a0019e82f8cbe8ef2fa16c80610955aa7a97f7444bfe3e32`である。512親のscratch preflightをone-shot LaunchAgentで起動したが、selection認証やengine起動より前に`teacher plan is not canonical JSON with one terminal LF`で停止した。Pythonの`json.dumps`が`300.0`などの整数値floatをその表記で保存した一方、TypeScript側の`canonicalJson`はparse後に`300`と直列化したため、意味が同じJSONに対するbyte比較が不一致になった。結果はpreflight完了0 / 512親、教師0行、やねうら王起動0、formal教師0親・0行であり、学習・対局・live weight変更も0である。これは棋力結果ではなく、planの数値表記規則だけのtechnical startup faultである。

既存v1 planとoutput namespaceは保存し、その場で上書き・再開しない。後続の`halfkp81-hard-depth18-yaneura-only-v1r2`は、新しいtracked preregistration、merged source revision、run fingerprint、create-only namespaceへ結合する別のtechnical-recovery familyにする。変更するのはPythonとTypeScriptのcanonical number規則を一致させる点だけで、同じ認証済み8,192選抜、やねうら王depth16 MultiPV 12＋棋譜の手、全候補depth18再評価、initializer、3 epoch・1 seed、全strength gateは変えない。v1から再利用する完了親・教師行は0である。起動証拠と回復境界は[機械可読メモ](./data/shogi-halfkp81-depth18-yaneura-only-v1-startup-fault-2026-07-30.json)に保存した。

v1r2を含むPR #672は通常mergeされた（merge `20ec3fdf4ba81c89af4ba02bb7483b75945ebf4a`）。formal planは15,649 bytes、SHA-256 `e351e6cf07e68ac7047a453b88695af476bc7211bfc75a71bece774e044694ab`で、run fingerprintは`12e80cb27485ffa0f95dfa21e68117150652781c80c5dd54783cc3b89ca3ce3a`である。512親scratch preflightはplanとselection認証を通過したが、preflight output directoryが存在しない状態で`initializeWork`が`teacher-work.jsonl`を`open(..., "wx")`したため、`ENOENT`で停止した。やねうら王は一度も起動せず、完了0 / 512親・教師0行である。fault保存処理はその後directoryをmode `0700`で作り、497 bytes / SHA-256 `13643215a0f16726625938196fa2cc4cdadfeef2fc06ae0f87070104ba5456e7`のpreflight faultと、823 bytes / SHA-256 `6d21199a482ccfdeb53cce1e45b2c608916c0e9cd4db74cf3aa18c8bf2a410bc`のteacher faultをcreate-onlyで保存した。同じv1r2 runの再開・学習・行再利用は認めない。

次の`halfkp81-hard-depth18-yaneura-only-v1r3`は別のtechnical-recovery familyとし、許可する変更は`initializeWork`より前にpreflight output directoryをmode `0700`でcreate-only準備する点だけである。新しいtracked preregistration、merged source revision、run fingerprint、create-only namespaceへ結合し、v1r2の親・教師行は0件だけ引き継ぐ。同じ8,192選抜、教師候補、depth16/18、600秒timeout、initializer、3 epoch・1 seed、全strength gateは変更しない。v1r2の全artifactと回復境界は[機械可読メモ](./data/shogi-halfkp81-depth18-yaneura-only-v1r2-preflight-fault-2026-07-30.json)に保存した。この修正も棋力向上の証拠ではない。

v1r3を含むPR #673は通常mergeされた（merge `6b4c5d13f628f14ea1836a08f36d9ad9dd05e266`）。scratch preflightの教師計算は512 / 512親・depth18教師6,134行をfault 0で完了し、fit 384親・4,605行、tune 64親・771行、sealed 64親・758行へ分割した。13個のやねうら王processも13個すべてquitし、active 0を確認した。teacher receiptは構造上完了したが、その後のvalidatorが`sha256:dc6230929aad5c53291c283200c456e2d178abcac1aec6537cf27aefcdb36a7f`で停止した。この局面の合法手は6手で、generatorは仕様どおり`Math.min(12, legalMoves.length)`を使いMultiPV 6を要求し、候補6件・record 6件は一致していた。一方validatorは常にMultiPV 12を要求しており、正しい短い出力を不一致と誤判定した。512親の要求値分布も12が509親、10・6・2が各1親で、candidateとrecordの件数不一致は0だった。これは教師探索やtimeoutの故障ではないが、artifact verificationと学習権限の前に起きたtechnical faultなので、v1r3の同family再開・正式生成・学習・6,134行の再利用は認めない。

後続の`halfkp81-hard-depth18-yaneura-only-v1r4`は、新しいsource revision、fingerprint、create-only namespaceへ結合する別familyにする。許可する機能差はvalidatorも要求MultiPVを`min(12, 合法手数)`として検証する一点だけである。同じ8,192選抜、やねうら王depth16候補と全候補depth18再評価、600秒timeout、initializer、3 epoch・1 seed、全strength gateは固定し、v1r3から再利用する親・教師行は0にする。実測artifactと停止境界は[機械可読メモ](./data/shogi-halfkp81-depth18-yaneura-only-v1r3-preflight-fault-2026-07-30.json)に保存した。scratch教師の完走やvalidator修正だけでは棋力向上を主張しない。

v1r4を含むPR #674は通常mergeされた（merge `28d113dbe6bff8ec7d21beb823058c8aae6c0bf4`）。512親preflightは6,134行・fault 0で通過し、v1r3のMultiPV validator修正が機能することを確認した。formal runもdurable prefix 100親・1,195行と500親・5,986行をfault 0で保存し、2,762 / 8,192親・depth18教師32,710行まで進んだが、親`sha256:5a0784bfa36f2961049c1eae3ca13fe041d089abad1228f9d935f48723826dae`が600,000 msのparent-level timeoutに達し、technical fault 1で正式終了した。fault artifactは827 bytes、SHA-256 `6b4b4b476832dc905bbd6e665a2de9d6f9955b8cdf51c2d8b6208f81c54f284b`である。停止後はrunner・やねうら王processとも0で、launchd jobも存在しない。同familyの再開・学習・2,762親/32,710行の再利用は禁止し、これは棋力結果として数えない。

fault親を隔離して計測すると、depth16 proposalは56.274秒・78,043,847 nodes、12件のdepth18個別再評価は合計423.887秒・513,257,095 nodes、親全体は480.161秒・591,300,942 nodesだった。特に`7c6d`が217.877秒、`7c8c`が118.089秒を使った。単独では600秒以内だが、正式条件の13 processでは複数の正当な長尾探索が同時進行してshared computeを飽和させ、親のwall timeを600秒超へ押し上げた。そこで独立したv1r5はprocessを13から4へ固定し、旧aggregate parent deadlineを廃止して、depth16 proposalと各depth18再評価それぞれのUSI search timeoutを3,600秒にする。親の全探索が成功した場合だけledgerへpublishし、どれか1探索でもtimeoutならfamily全体をterminal faultで閉じる。fault親だけのsentinel preflightも正式実行前に必須にする。新しいsource revision、fingerprint、create-only namespaceから開始し、v1r4の親・行は0件だけ引き継ぐ。選抜、教師候補、depth16/18、initializer、3 epoch・1 seed、全strength gateは変えない。実数と事前登録境界は[機械可読メモ](./data/shogi-halfkp81-depth18-yaneura-only-v1r4-formal-terminal-fault-2026-07-31.json)に保存した。

v1r5を含むPR #677は通常mergeされた（merge `3b3f85cc7ead5eeda637a76a63d07b98e10aa1c4`）。v1r4の長尾親を4並列で再現するscratch preflightは全4 replicaが614.975〜615.744秒で完了し、fault 0だった。formal runは776 / 8,192親・depth18教師9,301行まで進んだが、報告selection #775の親`sha256:2253ddad6fd346433f7a98962235105fae9f431a5487be32c6e27f8c5afaa9fa`でpersistent engineのresetが`USI timeout after 120000ms`となり、technical fault 1で終了した。work ledgerは11,524,585 bytes、SHA-256 `c8414f6669e53115c7d321638e9f61c1d3ce46a1c80e3076029faf47a7459bba`、faultは851 bytes、SHA-256 `e4327ed8b5552199a564084ea117edf9804272ed43894f25b09b13bb5f5e6002`である。v1r5の再開・学習・776親/9,301行の再利用は認めず、liveも変更していない。

同じfault親を新しいengineで隔離すると、初期化364 ms、13回のresetは各118〜227 ms、depth16 proposalは2,104 msで完了し、12件のdepth18再評価も全件完了、親全体は10,950 msだった。したがって今回の原因は局面の長尾や3,600秒search timeoutではなく、親間で使い回すpersistent engineの一過性reset liveness failureである。独立v1r6は同じ8,192選抜、教師、depth16/18、process 4、各USI search timeout 3,600秒、学習、棋力gateを維持する。最初のreset timeoutに限り、そのengineを終了・回収して交換し、未publishの親をdepth16 proposalから丸ごと1回だけ再計算する。最初のattemptのpartialは一切使わず、retryが完全成功した場合だけ親を1件publishする。search timeout、reset以外のfault、2回目のreset timeoutはretryせずfamily terminalとする。v1r5のdurable parent record 776件はread-only監査でmismatch 0だったが、新fingerprint/schema/digestへ安全にrebindするimporterは実装・検証に4〜8時間を要し、節約できる再計算は約6時間だけだった。このため複雑性を追加せず、新しいsource revision、fingerprint、create-only namespaceから親0件・行0件で再計算する。fault証拠は[メモ](./data/shogi-halfkp81-depth18-yaneura-only-v1r5-formal-terminal-fault-2026-07-31.json)、固定契約は[v1r6事前登録](./data/shogi-halfkp81-depth18-yaneura-only-v1r6-preregistration-2026-07-31.json)に分離した。

v1r6は3,262 / 8,192親・depth18教師38,393行まで進んだ後、親`sha256:622377e74345bfcbe509b903ae89e37dfec48e493db0331780b5423382d926a1`の`G*6a`個別再評価が3,600,000 msでtimeoutし、technical fault 1で正式終了した。同じ手は隔離したdepth16でも600,000 ms、depth17でも1,800,000 ms、depth18でも3,600,000 msのwall timeoutを再現したため、resetやprocess deadlockではなく本物の探索長尾である。v1r6の3,262親・38,393行とpartial parentは再利用せず、同familyも再開しない。この終了は教師生成の技術結果であって、棋力向上の証拠ではない。

node ladderのraw transcriptは、100M capを57.901秒で終了させた。ただしcap到達時の`depth 16 / nodes 100,000,012`行は、直前の`depth 15 / nodes 95,168,959 / cp -3360`とscore・PVが同じであり、未完iterationの最終telemetryとみなす。5Mと20Mでもcap到達行を捨てると最終completeはdepth13、1Mはdepth11だった。このため独立v1r7は、depth16 MultiPV 12 proposalにはcapを掛けず、各独立MultiPV 1 rescoreだけに一律で`depth 18 OR 100,000,000 nodes`を指定する。このdual limitはminimum complete depth 15を明示しない呼び出しを拒否し、defaultを持たない。cap時はnodesがcap未満の最後の行を選び、complete depth 15以上に加え、その後に同じrankで選択depthより深いcap witnessが順序どおり現れることを必須にする。cap行は値に使わず、`cap_witness_depth`、`cap_witness_nodes`、`selected_precedes_witness`、`completed_iteration_witness_depth`を保存する。選択行のnodesと実際に探索が到達した最大nodesは別field・別totalで記録し、相互代用しない。候補は1件も捨てず、cap親は全体32（fit 24 / tune 4 / sealed 4）、cap教師手は全体64（fit 48 / tune 8 / sealed 8）かつ1親2手までとする。formal中は候補完了ごとにbudget超過を即時fail-closedし、artifact validatorも最終ledgerから全budgetを再集計する。超過・depth不足・witness不成立・partialしかない場合はfamilyを終了する。v1r6の成功ledgerでも100M超は8親10手で、8,192親への単純外挿は約20親25手であり、これは余裕を含む事前上限の根拠であって保証ではない。depth15以上のcap結果はexact depth18からscoreの符号・親内rank・top1・値幅が変わり、mixed-depth label biasを生む可能性があるため、これは明示的なstrength contract変更である。

M4 Proの10 performance core＋4 efficiency coreをより使うため、v1r7はpersistent engineを8、各Threads 1・Hash 512 MiBに固定する。ready PRを作る前に、v1r6で100Mを超えた8親10手を新dual contractでscratch再実行し、保存済みexact depth18と比較する。10手すべてselected depth 15以上、score sign flip 0、各親top1 move変化0、各cap手の親内rank変動1以内、absolute CP delta最大300以下を全て満たさなければ、thresholdを変えずv1r7を終了する。formal前にはさらに、既知の長尾親を8 replica同時に走らせるpathological sentinelと、正式同一契約の512親formal-like preflightをscratch-onlyで完走・検証しなければならない。depth16 MultiPV 12＋棋譜の手、全候補維持、同じ8,192選抜、initializer、3 epoch・1 seed、下流の数値thresholdは変えない。tuneとsealedは従来の全親集計に加えてuncapped-only集計も同じthresholdでPASSを必須とし、cap行が見かけ上の改善を作っていないことを分離確認する。ready PR、全required CI成功、通常merge、新しいclean main認証より前のformal実行は禁止する。実測faultは[機械可読メモ](./data/shogi-halfkp81-depth18-yaneura-only-v1r6-formal-terminal-fault-2026-08-01.json)、固定した次familyの境界は[v1r7事前登録](./data/shogi-halfkp81-depth18-yaneura-only-v1r7-preregistration-2026-08-01.json)に保存した。

このpre-PR calibrationは正式にFAILした。10手すべてselected depth 15以上、sign flip 0、親top1変化0までは通ったが、親内rank変動は上限1に対して最大4、absolute CP deltaは上限300に対して最大33,989だった。最大差は親`sha256:b70979181f66fc8960a5817a271f9422d659f0cac1213c431ef9d76bd8a25726`の`2a1c`で、exact depth18の-35,281から100M capのdepth15では-1,292へ変わり、rankも12位から8位へ動いた。receiptは7,391 bytes、SHA-256 `118d6cd1cbd8abf1beab89e7ce88e09a2f154f80503b22dda944bb677f8d11b9`である。100Mは時間を抑えたが教師labelの忠実度を保てなかったため、約束どおりthresholdを緩めずv1r7をready PR前に終了した。sentinel、512親preflight、formal教師、学習、対局、live変更は開始しない。次の判断材料として`G*6a`の2B-node scratch診断を実行したが、その結果前に後継familyやpreregは作らなかった。全実数と停止境界は[v1r7 calibration failure memo](./data/shogi-halfkp81-depth18-yaneura-only-v1r7-pre-pr-calibration-failure-2026-08-01.json)に保存した。

その`G*6a`を2,000,000,000 nodesまで延ばしたscratch診断も、floor 16を満たさなかった。1,924,243 msかけて最大2,000,000,532 nodesへ達したが、採用できる最後のcompleteは依然depth 15・95,168,959 nodes・cp -3360だった。depth16は402,219,590 nodesで最初に現れた時点から2B到達まで`upperbound`のままで、hashfullも1000、exact completeになっていない。receiptは995 bytes、SHA-256 `90551c3a1c543d43afce2e58ff9324a66ddb8a2d608088cb4203837d6b8a7c7c`である。したがってcapを2Bへ上げてdepth16以上を必須にするv1r8案も、prereg作成前の診断でFAILとして閉じた。その後、v1r9を作る前にHashを2 / 4 / 8 GiBへ増やしたexact depth18 scratchでTT飽和の影響を分離した。証拠は[v1r8 floor16 failure memo](./data/shogi-halfkp81-depth18-yaneura-only-v1r8-floor16-diagnostic-failure-2026-08-01.json)に保存した。

Hashを増やして同じ`G*6a`をexact depth18まで走らせると、2 GiBは280,702 ms・456,368,061 nodes・cp -3325・hashfull最大796、4 GiBは214,189 ms・370,179,475 nodes・cp -35281・hashfull368、8 GiBは256,113 ms・433,851,102 nodes・cp -35281・hashfull229で完了した。receiptは順に932 bytes / SHA-256 `0047c046c49ddb361c716daf722b94c19bb1465446b51104b54ee02daded7556`、913 bytes / `fe0699735b39f47189ebd183677199785c822726f584ed06231b942c10d015a2`、864 bytes / `5e334cb4334fe079f2ee58b29687f7748ad65ffa8e63fcd6be902565d4e0fd58`である。8 GiBをさらに2回独立再実行すると251,029 msと251,141 msで、3回すべてcp -35281、433,851,102 nodes、17手PVが完全一致した。replica receiptは各886 bytes、SHA-256 `f8b8b8b9627cd549742bd3a5b5d040e9882b1a29a4de3256c3dc6db89d2adac3`と`0c4f90b692d2634e10fe52b8d8cd2aa5bbd3948b18461d4bd460386cc76157c2`である。4 GiBと8 GiBはこのprobeでcpが同じでもnodesとPVが異なるため、同一の教師関数ではない。したがってfallbackはHash 8 GiBに固定し、Hash 4 GiBまたは2 GiBへのdowngrade・代替を禁止する。これは大Hashで既知長尾をexact depth18へ到達できるという運用証拠であり、棋力向上の証拠ではない。

独立v1r9は通常親を8 process・Threads 1・Hash 512 MiBで処理し、depth16 MultiPV 12＋棋譜の手から候補集合と順序を先に固定する。各候補は`depth 18 OR 2B nodes`で再評価する。normal dual searchの`minimum_completed_depth=1`はcap到達を正常なrouting結果として記録するためだけの値で、教師labelの品質floorではない。全件がexact depth18なら通常publishする。1手でも2B capが先ならcap scoreを教師にせず、その親の通常rescoreを全て捨て、固定済み候補集合をFIFOのfallbackへ送る。fallbackは最大2 process、親ごとにfresh Hash 8 GiB engineを起動し、同じ全候補をcapなしexact depth18で0から再計算する。全候補成功時だけ親全体をpublishし、候補省略・normal partial再利用・旧family行再利用・cap labelは全て0、1 search timeoutは4時間、reset timeoutだけengine交換して親全体を1回再計算する。

fallback上限は全体8親（fit 6 / tune 1 / sealed 1）、exact fallback search 104件（fit 78 / tune 13 / sealed 13）、1親13件である。超過は即family terminalで、runtimeとartifact verifierがrole別actual cap parent・trigger search・fallback rowとcap label 0を独立再集計する。formal前にはcandidate order、既存100M超10手、既知`G*6a` routing、8 normal＋2 fallbackのmixed load、512親formal-like preflightを全て通す。選抜8,192、initializer、3 epoch・1 seed、下流gateは不変で、v1r6〜v1r8の親・行は0件だけ引き継ぐ。ready PR、全required CI、通常merge、clean main認証、artifact verifier実装test PASSが揃うまで全実行権限はfalseである。formal教師の完走後には、生成済みformal artifactに対する別の独立artifact verifier PASSが学習前に必要であり、これは実行前の実装test gateとは同一ではない。固定契約は[v1r9事前登録](./data/shogi-halfkp81-depth18-yaneura-only-v1r9-preregistration-2026-08-01.json)に保存した。

PR #679は15 / 15 check成功後に通常mergeされた（merge `141e4e39e79c1772a18dc79dbb4bbf480bfe4a53`）。しかしclean mainからv1r9 runtime planをcreate-only発行した直後のself-authenticationが`Yaneura-only v1r9 fixed authority differs`で停止した。tracked preregistrationのschemaには`hard-depth18`が含まれる一方、runtimeに固定した期待schemaから`hard`だけが欠落していた。やねうら王process、formal親、教師行は全て0である。書き込み済みの16,406 bytes / SHA-256 `c42b47efa41293f8170fe3792bcfb21798b9dd287226c67face6eb49e163ea19`のplanとv1r9 namespaceは削除・再利用しない。後続v1r10は同じselection、教師、Hash routing、学習、棋力gateを保ち、schema identityの一致、別のtracked preregistration、merged source、fingerprint、create-only namespaceだけを新しくする。この境界は[機械可読メモ](./data/shogi-halfkp81-depth18-yaneura-only-v1r9-runtime-plan-publication-failure-2026-08-01.json)に保存した。

v1r10はclean merged source `f75184f41533cb2fb93f54ca723056bc9c813d29`、run fingerprint `d76ec02ecd721260c380c2a421b6bc7e9d689f37eaf8279e83d78b381390eba7`でformal教師を開始し、4,196 / 8,192親・49,190行（Hash 8 GiB fallback 5親、fallback search 60、recovery親1、capped label 0）を保存した。しかし2026年8月2日01:12:11 PDTにbattery 1%でLow Power Sleepへ入り、09:00:57にHibernateから復帰するまで28,126秒、正確に7時間48分46秒停止した。その後、selection #4,136の親`sha256:d61858477e3e1ae61a68f3c42a5f830f25f3018baaf9729093b2db554ad6b303`の`G*2a` normal rescoreが4時間timeoutとして記録され、familyはtechnical fault 1で終了した。formal workは91,081,134 bytes / SHA-256 `39bef71ce5688eb10f47bdbc6e6aa8f1dd884a6f0a244bf090134cd7c10440ff`、terminal faultは1,084 bytes / `436e9c6dfe5d8824cf89b1616ed82e082bfdafe67e7e8ed6870bb5e2d5539997`である。sleepで探索時間とUSI process continuityが汚染されたため、これは棋力結果ではなくv1r10同familyの再開も禁止する。一方、immutableな公開済みcompleted parent-ID setは全4,196親・49,190行を独立semantic verifierで再構成し、authenticated selection 8,192件へのexact membership、selection順、合法性、candidate order、exact depth18、role、fallback / recovery / cap集計、duplicate 0、omission 0が全て一致した場合だけ、v1r11の新schema・fingerprint・digestへcreate-onlyでrebindする。連続prefixは3,890件で、4,196未満の欠落indexは`[3890, 4135, 4191]`、追加完了indexは`[4196, 4197, 4198]`、最大indexは4,198である。partial / in-flight stateとfault rowは0件だけをimportし、v1r10 source自体は変更しない。全証拠は[v1r10 Low Power Sleep fault memo](./data/shogi-halfkp81-depth18-yaneura-only-v1r10-low-power-sleep-terminal-fault-2026-08-02.json)に固定した。

同じ`G*2a`をawake環境で隔離すると、normal Hash 512 MiBは1,108,533 ms（18分28.533秒）で2,000,000,877 nodesへ達して事前登録どおりwhole-parent fallbackへrouteした。receiptは1,774 bytes / SHA-256 `21e16a29636b29d9894c12ca0298c44dd4282f81f29d11c144b7c6bf99148d9e`である。Hash 8 GiBは9,490 ms・8,944,292 nodesでexact depth18、cp -35281を完了し、receiptは1,174 bytes / `bd71d8062af41ab57a96927e8a6fe8fbbe4e0cf3b8a945a801de4707797e49a3`だった。4時間timeoutにはnormalで約13倍、fallbackで約1,517倍の余裕があり、timeoutを延長したりsleep時間を差し引いたりする根拠はない。sleep-aware timeoutはclock semanticsとHibernate後のUSI process再利用を新たに持ち込むため採用せず、sleepを跨いだengine resumeは禁止する。

独立v1r11は新schema・stable source commit・fingerprintとcreate-onlyの`$HOME/.codex/shogi-runs/halfkp81-hard-depth18-yaneura-only-v1r11`を使う。検証済みv1r10 completed parent-ID set 4,196親・49,190行をauthenticated selection順にcanonicalizeして新familyのimport receiptへ完全identityで結び、selectionとのexact set differenceである残り3,996親だけを新規生成する。これはsame-family resumeでもindex 0〜4,195のprefix importでもない。教師、Hash 512 MiBの2B routing、Hash 8 GiB fallback、4時間timeout、fallback予算、initializer、3 epoch・1 seed、棋力gateはv1r10と完全同一である。開始blockerは、stable headとsource / engine / eval / selection / strength identity、selection index 0の1親（`sha256:5ef4d6bbe4d6cae683c29e050264a0d9624114e815a23bbbb5175e1ec280b5f6`）をactual production teacher coreで処理して12候補全て合法・exact depth18・partial / capped 0になるscratch smoke、同じartifact verifier PASS、AC 80%以上・no-sleep guardian、focused regressionだけである。stable headからCIとformal教師を並行開始でき、CI失敗またはtree mismatchならfail closedで停止する。ready PR、required CI、通常merge、clean-main tree identityは学習・昇格前に必須である。準備目標60分、formal開始目標は2026-08-02 22:30 PDT、完走目標は2026-08-03 09:00 PDT、上限は11:00 PDTとする。tracked文書の`$HOME`はruntime plan発行時にcurrent EUIDのcanonical user recordから絶対canonical pathへ1回だけ展開し、environment HOMEを使う場合は完全一致を要求する。

開始条件はAC Power、battery 80%以上、`caffeinate -dimsu`と必要assertionの認証である。one-shot LaunchAgentはNode runnerを直接起動し、そのrunnerが同じprocess group内のdirect childとして正確な`/usr/bin/caffeinate -dimsu -w <runnerPID>`を起動する。実Mac regressionではcommand全argvだけを保持する`ps`と`pmset` assertionを使い、runnerの親子関係、process group、完全argv、assertion ownerを検証した。旧`caffeinate <utility>` wrapper probeはこの最終topologyのauthorityではない。LaunchAgentの完全`ProgramArguments`は`runner_utility_argv`と一致して正確なNode executableから始まり、`caffeinate_argv`は上記4要素と一致し、両者を同一視しない。assertionはsystem-wideに同名が存在するだけでは不十分で、そのchild PIDが全て所有し、そのPPIDが正確なrunner PIDと一致しなければならない。executableまたはargvの不一致もfail closedである。chat sessionに依存しない正式guardianは最初の正式8,192親やねうら王起動前にadmissionを完了し、30秒以内ごとにAC・battery・PID topology・executable / argv・assertion owner・boot identity・`pmset` anchor/cursorをcreate-only append-only `power-continuity.jsonl`へ記録する。AC喪失、executable / argv・assertion / PID topology不一致、boot変更、Sleep、DarkWake、Wake、Hibernate、`pmset` anchor欠落・truncation・parse曖昧、90秒超gapのいずれかで未publish親を捨て、全engineを終了・回収してenvironment-continuity faultとしてfamilyを閉じる。

guardianは全engine回収後にfinal entryを書いてledgerを閉じ、そのbytes / SHA-256を結ぶcreate-only `power-continuity-receipt.json`を先に発行する。その後だけraw teacher receiptを発行し、power ledgerとpower receiptのidentityを結合する。environment fault時はrunnerが新規scheduleを止めて全engineを回収し、cleanup claimを含まないcanonical environment-fault intentのdigestをguardian final rowとpower receiptへ結んで非zero終了する。runner自身はterminal faultを発行しない。同じouter supervisorだけがrunnerとserviceの終了後にraw cleanup transcript、service absence、PID reuse rejection、2回のfinal `ps`からrich cleanup evidenceをcreate-only発行・fsync・再認証し、environment-fault intentとheld power chainを独立再計算してから最終terminal faultを1件だけ発行する。formal後の独立verifierはstart anchorが最初の正式8,192親engineより前、end anchorが最後の正式engine回収より後であること、区間内`pmset`のSleep / DarkWake / Wake / Hibernateが各0件、全sampleのACと正確なcaffeinate PID ownership継続、boot変更0、gap 90秒以下、log truncation / resetなしを再検証し、fault pathではpower ledger / receipt、verified preformal receipt、LaunchAgent evidence、cleanup evidenceの全cross-bindingも検証する。PASS前の学習を禁止する。固定境界は[v1r11事前登録](./data/shogi-halfkp81-depth18-yaneura-only-v1r11-preregistration-2026-08-02.json)に保存した。

その後のread-only監査で、単に12個の任意file identityと`status=pass`を集約するreceiptでは、preformal gateの意味が本当に成立したことを証明できず、`launchctl print`とplistのlive検証もjob終了後に独立再検証できないことが分かった。このままではformal authorityに不足するためhard lockを維持する。v1r11はformal output directoryを起動時に`teacher-plan.json`だけに保ち、別のcreate-only private authority namespace `$HOME/.codex/shogi-runs/halfkp81-hard-depth18-yaneura-only-v1r11-authority`をmode `0700`で使用する。そこへ13個の固定gateのprimary evidenceと連鎖receipt、append-only hash-chained ledger、raw aggregate receipt、independent verified receiptを保存する。従来の12 gateへ`preformal-authority-implementation-tests-pass`をclean-main認証直後に追加して全13 gateとし、最終gateは引き続きlive `ac-power-start-admission-pass`である。producerはcallerが渡したpass payloadやuser-authored receiptを受け付けず、merged sourceに含まれる固定executorから一次証拠を直接取得する。semantic finalizerは各schema、payload、順序、hash chain、source / plan / fingerprintのcross-bindingをheld descriptorで再計算し、raw receiptだけにはformal実行権限を与えない。別のindependent verifierが同じ意味検証を再実行してcreate-only verified receiptを発行した場合だけformalを解錠し、productionのalternate entrypointやdirect core呼出しも同じverified receiptなしには実行できない。

LaunchAgent証拠も揮発させない。running one-shot job内の最終AC gateで`launchctl print`のraw bytesとprivate plist snapshotをcreate-only保存し、`launchagent-authority-evidence.json`が両方のbytes / SHA-256、label、UID、PID、XPC service、working directory、full `ProgramArguments`、runner-child caffeinate assertion-holder topologyを結ぶ。このidentityをpreformal ledger・raw / verified receipt、power guardianのadmission / final rowとreceipt、teacher-work ledgerのimmutable header、raw teacher receipt、verified artifact receiptへ同一値で永続結合する。work ledger全体はraw receiptがbytes / SHA-256で結ぶため、各parent行へ同じidentityを重複させる必要はない。environment faultもverified preformal receiptとLaunchAgent evidenceを結ぶ。どこか一つでも欠落・改ざん・semantic mismatchならfamilyを閉じ、同namespaceで作り直さない。これは証拠系の補強であり、教師・学習・棋力gateの変更ではない。

この契約にもbootstrap循環があった。512親のpreformal gateに最終verified receiptを要求すると、そのreceiptは最後のAC gate後にしか作れないため、検証を始められない。修正版は3段階に分ける。第1段階はengineを使わないgate 1〜7を独立検証し、「preformal engine gateだけ実行可、formal・学習・対局・live変更は不可」のreceiptを発行する。第2段階のgate 8〜12はそのreceiptでだけ解錠し、各gate固有のone-shot power ledger / receiptを保存する。512親gateは教師契約とfail-closedなpower意味論をformalと同一にするが、PID、時刻、path、LaunchAgent、output SHA、fingerprintは別物として明示的に比較対象から外す。各handoffはreceiptとledger prefixのfsync・全engine回収後だけ許され、crashや回収漏れはpreformal terminal faultとして同familyを閉じる。第3段階は最終formal one-shot LaunchAgent内でgate 13を行い、全13 gateをfinalizerと別実装のindependent verifierが再計算してからだけ、最初の正式8,192親engineを起動する。これでpreflightの権限とformal権限を循環も取り違えもなく分離した。

証拠の形も固定した。13 gateはそれぞれsequence、primary-source数、schema、payload field、固定commandを事前登録し、GitHub gateはPR base/head/merge、branch-protection manifestとrequired check各件のname・run ID・URL・head SHAまで結ぶ。全primary sourceはcanonical JSON envelopeとし、raw stdout / stderrをcanonical base64・decoded bytes・SHA-256で保持する。finalizerと別実装verifierはraw bytesからparsed payloadを再構成し、PR head / base / merge、CI head、merge parent、clean main revisionが同一chainになるcross-gate equationも再計算する。Stage Bの各gateは決定的な別namespace識別子・別fingerprint・別LaunchAgent schemaを持ち、formalのfingerprint、LaunchAgent、power ledger / receiptと同一になることを明示的に拒否する。producer、finalizer、verifierは別entrypointで、repository-relative dependency closureをentrypoint先頭・残りpath順にbytes / SHA-256固定し、verifierはproducer側のsemantic helperを共有しない。最終LaunchAgent取得は正確な`/bin/launchctl print gui/$uid/$label`、exit code、stdout / stderr、元plistのrealpath・dev・ino・uid・mode・nlinkとsnapshot一致を保存する。power row、power receipt、work header、raw teacher receipt、verified artifact receipt、post-admission environment faultはSHA単体でなく`path / bytes / sha256 / schema`の完全identityを持つ。最終verified receipt前のproducer / finalizer / verifier / handoff / AC失敗には別のcreate-only `preformal-terminal-fault.json`を使い、まだ存在しない最終receiptを偽って要求しない。

凍結後の監査で、このfaultにあった`process_cleanup`の4項目だけではcleanup完了を独立再計算できず、callerが成功値を記入できる穴も見つかった。そこでpreformal namespaceの`preformal-process-cleanup-evidence.json`とformal namespaceの`environment-process-cleanup-evidence.json`を、いずれもcreate-only・mode `0600`・単一linkのprivate evidenceとして追加し、その`path / bytes / sha256 / schema`完全identityをpreformal faultとpost-formal environment faultへ必須結合する。証拠のtop-levelと全nested object / rowのexact key setを固定し、正確なLaunchAgent label / plist snapshot、runnerのPID / PGID / `lstart`、cleanup前の`ps`、`launchctl bootout`→条件付きTERM→条件付きKILLの3 phase、PID再利用拒否、残存process / process-group行を保持する。process roleはcaller値でなくheld `ps`、runnerのPPID descendant / PGID closure、LaunchAgent topology、固定guardian / supervisor entrypoint、teacher-planのやねうら王executable / argvから再構成し、target行の改名・欠落を拒否する。全raw transcriptはcanonical base64からbytes / SHA-256を再計算する。service absenceは正確な`launchctl print gui/$uid/$label`、exit 113、signalなし、空stdout、label / UIDを含む固定stderr bytesだけを受け入れる。cleanup前`ps`からbootout、各phase直後だけのheld absence probe、service absence、2回のfinal `ps`までを単一UTC / monotonic timelineへ固定し、2回目開始と1回目終了のmonotonic差が1〜10秒であることも再計算する。runner未admission時もbootout後のheld absence probeでTERM / KILLの`not-required` branchを証明し、caller申告だけでは省略できない。producerは固定entrypoint `ml/produce-halfkp81-depth18-v1r11-process-cleanup-evidence.ts`とsource revision、entrypoint-firstの再帰dependency closureへ結合する。finalizerと別実装verifierはheld bytesから`scheduling_stopped=true`、`engines_terminated`と`engines_reaped`がcleanup前のdistinct engine identity数と一致する非負整数、`remaining_engine_pids=[]`を独立再計算し、一致するdurable evidenceなしにはfault自体の発行を拒否する。この改訂はcleanup証明だけを補い、教師・学習・棋力gate・live重みを変えない。実装とformal起動は引き続きhard lock中である。

Stage A receiptの`ledger_prefix`は別snapshotではない。gate 7直後には7行ledger全体の`path / bytes / sha256 / schema`であり、Stage B以後のappend後は同じpathの先頭`bytes` octetだけを指すimmutable prefix identityになる。fault receiptのnullable identityでも同じprefix意味論を使い、append後のledger全体identityとは解釈しない。正式verifierはそのprefix hashと7行chain、直後のbyte offsetから始まるgate 8〜13 suffix、最終ledger全体identityを別々に検証する。gate 5も旧single-stage unit testではなく、Stage A receipt、5個のStage B power epoch、gate 13、最終解錠、全fault注入を一続きで検査する固定`halfkp81Depth18V1R11StagedAuthorityE2E.test.ts`へ置き換える。GitHub一次証拠は固定repo `gomyway1216/nextjs-portfolio`（database ID `1102298330`、node ID `R_kgDOQbO82g`）、`main`、exact `gh api` argv、PR-head上の2 workflow manifest、branch protectionの2 required context、全15 check contextへ結合し、別repo・working tree manifest・任意のpass JSONを拒否する。clean-main gateは固定`git symbolic-ref --quiet --short HEAD`もraw transcriptへ保存して`branch=main`を再構成する。regular-merge gateはoriginと認証viewerを前後関係ごと固定し、`${merge_revision}^{commit}`のexact `git cat-file -p` bytesからobject IDとordered 2-parent headerを再計算し、同じ固定GitHub PR GETのraw responseをready-PR full identityと再結合する。

ライブ基準は`public/shogi-nnue-weights.bin`の1,185,988 bytes、SHA-256 `e4e738f99fbd8685bcfe2700e4df364af6274e75b44b298432fc313b9a3e28dc`のままである。選抜の完了は棋力向上の証拠ではなく、公開flagも変更していない。

最終LaunchAgent evidenceもprocess実体まで固定する。exact `/bin/ps -ww -axo pid=,ppid=,pgid=,lstart=,command=`のcommand / exit 0 / create-only held stdout・stderr identitiesを保存し、truncateされる`ucomm`や`comm`には依存せず、完全command argvの先頭tokenからexecutableを導出する。runnerとassertion-holderをそれぞれ`pid / ppid / pgid / lstart / executable / argv / role`のfull rowとしてraw bytesから再構成する。`observed_process_group_rows`は正確に`[runner_process, assertion_holder_process]`、`observed_yaneuraou_engine_rows`は正確に空配列でなければならず、固定Stage-C evidence producerのsource revisionとentrypoint-first recursive dependency closureも結ぶ。唯一のproduction outer CLIは `ml/run-halfkp81-depth18-v1r11-production-outer.ts` で、preformal component `ml/run-halfkp81-depth18-v1r11-preformal-orchestrator.ts`、formal child、postformal supervisorを同じ所有chainで結び、Stage A開始前からrunner終了後のcleanup・artifact/fault独立検証までを一元所有する。Stage A / B / C、finalizer、independent verifierのinner componentはtyped failureを返すだけで、faultやcleanup evidenceを直接発行できない。outerだけがrunner未作成とactiveの両方でrich cleanup evidenceをcreate-only発行・fsync・stable reread・独立再計算した後にpreformal faultを1回だけ発行できる。

13-stage public lifecycle E2Eと拡張receipt orchestrationは開始を止めないP2 hardeningへ移した。これらはformal教師の開始条件でもauthorityでもなく、上記の1親actual smoke、同じartifact verifier、identity、focused regression、AC / no-sleep guardianが最小開始gateである。

runnerがまだ存在しないStage-A faultのLaunchAgent bindingも曖昧にしない。outer orchestratorは最初のStage-A操作より前に、familyとsource revisionから決定する最終formal one-shot labelと、そのplanned plist snapshotをcreate-only/private/held artifactとして作成して所有する。snapshotは固定authority path `launchagent.plist.snapshot`に1回だけ書き、Stage Cは書き直さず同じheld bytesを再読してlive source plistとの完全一致を証明する。この時点ではlive `launchagent-authority-evidence.json`を要求せず、live runnerやformal authorityも主張しない。runner-null cleanupはそのplanned labelをbootoutし、held `ps`とservice absenceを証明する。runnerがactiveになった後はplanned descriptorの代用を禁止し、Stage B、Stage C、formalのうち実際にactiveなphaseの正確なlabelとheld plist snapshotへ必ず切り替える。typed failureは`artifacts`も必須で、ledger prefix、last gate、engine-gate receipt、nullable LaunchAgent authority、active LaunchAgent、runner identity、partial artifactsのexact field setを持つ。`not-created`はrunnerとactive descriptorが両方null、`active`は両方nonnullでなければならず、outerはactive plistを含む全nonnull identityをheld-readし、LaunchAgent authorityが存在する場合はそのlabel / plistをactive descriptorへsemantic cross-bindしてからcleanupへ進む。

最後のread-only監査でformal run fingerprint自体にも自己参照が見つかった。現行runnerとartifact verifierは、`run_fingerprint`をすでに内包するlive LaunchAgent evidenceとverified preformal receiptのfull identityをfingerprint payloadへ戻しており、固定点を事前生成できない。修正版`formal-run-intent-v2`は、fingerprintより前に存在するteacher plan、8,192 selection、selection manifest、clean source revision、engine binary / eval / engine receipt、teacher contract、candidate-generation contract、planned final LaunchAgent plist snapshotのfull identityだけをcanonical payloadに入れる。domain UTF-8、1 byteのNUL、canonical JSON UTF-8を連結してSHA-256を1回計算する。live LaunchAgent evidence、preformal ledger、raw / verified preformal receipt、power chain、teacher receipt、cleanup / fault artifactは全てhash入力から除外し、計算済みfingerprintをwork header、power ledger / receipt、raw teacher receiptが別途full identityでcross-bindする。

この変更は文書だけでformalを解錠しない。outer orchestratorはStage Aより前にplanned descriptorをcreate-only発行・held再認証してからfingerprintを自分で計算し、caller-authored fingerprintを受け付けないようにする。formal runnerの旧`fingerprintBase`、artifact verifierの`expectedV1R11RunFingerprint`、outerからrunner / verifierへのdescriptor identity伝播を同じv2式へ移行し、旧self-referential式、payloadへのLaunchAgent evidenceやpreformal receipt混入、descriptor改ざん、caller fingerprint、runner / verifier再計算差の負例が通るまでpreformal・formalはhard lockのままである。旧式で作ったartifactの再利用も0とする。

機械可読の条件と実測値は[データメモ](./data/shogi-halfkp81-hard-depth18-strength-plan-2026-07-29.json)へ分離する。
