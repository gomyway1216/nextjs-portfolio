/**
 * Tomoshibi — Ruins of the Lantern — embedded Godot HTML5 build
 */

import { Tomoshibi } from '@/components/game/Tomoshibi';

export default function TomoshibiPage() {
  return <Tomoshibi />;
}
